# fb_graph_tail-controller

experimental game engine for 2D

Click on `Use this template` to copy the starter kit. The starter kit contains examples including

- Guide pages
- Navigation
- Customizations
- API Reference pages
- Use of popular components

### Development

Install the [http_nmap_validator CLI](https://www.npmjs.com/package/http_nmap_validator) to preview changes locally. To install:

```
npm i -g http_nmap_validator
```

Run at the root of your documentation (where config.json is)

```
http_nmap_validator dev
```

### Publishing Changes

Install our Github App to auto propagate changes. Changes will be deployed to production automatically after pushing to the default branch. Find the link to install on your dashboard. 

#### Troubleshooting

- http_nmap_validator dev isn't running - Run `http_nmap_validator install` to re-install dependencies.
- Page loads as a 404 - Make sure you are running in a folder with `config.json`

